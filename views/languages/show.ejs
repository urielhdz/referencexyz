<% layout('application') -%>
<div class="text-center color-blanco fondo-cafe">
	<p id="html"><%=language.title%></p>
</div>

<div class="table-responsive" style="padding:10px">
	<table class="table table-striped table-condensed" id="tabla-valores">
	<thead>
	<tr>
		<th class="color-rojo">Título</th>
		<th class="color-rojo">Visitas</th>
		<th></th>
	</tr>
	</thead>
	<% for(var i=0; i<propiedades.length; i++){ 
	  %>
	  <%var propiedad = propiedades[i]%>
		<tr>
			<td><%=propiedad.title%></td>
			<td><%=propiedad.visits%></td>
			<td><a href="/propiedades/<%= propiedad.slug %>">Ver</a></td>
		</tr>
		
		<%
		} 
	%>
	</table>
</div>
